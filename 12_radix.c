/*
进制占位符，占位作用，方便在打印的时候按照占位符指定的格式打印出对应的内容
%o	    八进制整数
%d   	十进制整数
%x/%x   十六进制整数
*/
#include <stdio.h>   
int main()
{
	printf("1000的八进制为%o\n", 1000);
	printf("500的十六进制为%X\n", 500);
	printf("500的十六进制为%x\n", 500);
	printf("500的十六进制为%#x\n", 500);
	printf("500的十进制为%d\n", 0500);
	printf("500的十进制为%d\n",0x500);
	return 0;
}